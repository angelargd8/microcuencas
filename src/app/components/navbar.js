const navbar = ({children, className, style, onClick}) => {
    return (
      <div className={`navbar ${className}`} style={style} onClick={onClick}>

      </div>
    );
  };
  
  export default navbar;